// import { useUserAccount } from "./useUserVaults";

// export const useCheckAccountExistence = () => {
//     const { account, isLoading, error } = useUserAccount();

//     // Account exists if account object is not null and has an id
//     const hasAccount = account !== null && !!account.id;

//     console.log("Account existence check:", { hasAccount, account });

//     return {
//         hasAccount,
//         isLoading,
//         error,
//         account,
//     };
// };